/**
 * Tailwind CSS v4 config (CSS-first).
 * In v4, most configuration lives in your CSS via @theme and @plugin.
 * This file is intentionally minimal to avoid v3-only options causing errors.
 * Add JS config here only if you really need it.
 */

/** @type {import('tailwindcss').Config} */
export default {

};
